DESTITUTE,
    var = + 2 2 #polish notation for arithmetic
    
    ||| var ?? 4, #if var == 4
        why would you say "4"
    
    || var ?? 2, #else if var == 2
        why would you say "2"

    |, #else
        why would you say "Something Else"
WEALTHY
